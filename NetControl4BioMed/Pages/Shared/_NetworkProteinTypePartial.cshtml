@model NetworkProteinType
@using NetControl4BioMed.Data.Enumerations
@using NetControl4BioMed.Helpers.Extensions

<div class="text-uppercase">
    @switch (Model)
    {
        case NetworkProteinType.None:
            <span style="color: lightgray;"><i class="fas fa-circle"></i></span>
            break;
        case NetworkProteinType.Seed:
            <span style="color: tomato;"><i class="fas fa-circle"></i></span>
            break;
        default:
            break;
    }
    <span title="@Model.GetDisplayDescription()">@Model.GetDisplayName()</span>
</div>
